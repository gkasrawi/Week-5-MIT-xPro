Array/String Manipulation.slice Method 

const numbers = [4, 7, 1, 3, 5, 4, 9, 23]

console.log(numbers); //[14, 7, 1, 3, 5, 4, 9, 23]
console.log(numbers.slice(0, 3)); //[4, 7, 1]
// To remove an elements units of within an array or string, utilized the .slice method.
//The slice() method extracts a section of a string and return it as a new string without modifying the orginal string.