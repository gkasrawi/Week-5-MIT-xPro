Functions: Array/String Manipulation.indexOf Methods
const myName = Gabriel

console.log(myName,indexOf('b'));    // returns the index 3
console.log(myName.lastindexOf('i'));// returns the index 5
console.log(myName.indexOf('z'));    // returns the index -1
//The indexOf() method returns the index within the string of the first occurrence of the specified value, starting the search at from Index.
//It returns-1 if the value is not found. 